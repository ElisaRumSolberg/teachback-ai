# Set exactly one provider's key. LLM_PROVIDER is inferred from whichever is set,
# or set it explicitly if you have more than one key in your environment.
OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
# GEMINI_API_KEY=
# LLM_PROVIDER=openai

PORT=3000
CONSISTENCY_RUNS=5
